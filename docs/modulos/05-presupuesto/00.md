# `05-presupuesto` — planificación I-07

> **Estado (2026-09-01):** Plan 015 + Plan 019 + Plan 020 + Plan 021 +
> Plan 022 + Plan 023 + Plan 024 + Plan 025 **DONE**; módulo `presupuesto`
> I-07 cerrado. Plan 025 entrega el endpoint P-32
> (`GET /presupuestos/{presupuestoId}/validacion`), los DTOs
> `ValidacionPresupuestoResponse` + `RubroRefResponse`,
> `PresupuestoRepository.findRubrosCubiertosPorCronograma` (SQL nativo
> narrow, mirror del deep copy Plan 024 — sin entidad JPA
> `Actividad`/`Cronograma`), `PresupuestoValidacionResourceIT` 13/13,
> Bruno `api/bruno/10-presupuesto/` autocontenido (23 requests,
> 23/23 verde dinámico contra PostgreSQL 18 limpio + fast-jar) y
> cierre documental sincronizado. **Sin commit unitario ni merge**
> (instrucción explícita del orquestador). Evidencia medida:
> presupuesto 98/98, APU 52/52, recalculo 4/4, motor 45 = 42 pass +
> 2 aceptados (GM-19 `-$6.95`, GM-20 cap. 1 `-$0.84`) + 1 skipped
> (GM-24 `@Disabled`) + 0 errors; suite completa **438 = 435 pass +
> 2 aceptados + 1 skipped + 0 errors**; Spotless PASS; `build -x
> test` PASS; `git diff --check` limpio. `graphify update .` final:
> 2.972 nodos / 9.064 aristas / 141 comunidades. La **siguiente tarea de planificación**
> es Plan 026 (I-08 — cronograma CRUD), que aún **no** existe como
> archivo ejecutable y debe autorarse en su propia sesión. Índice del
> módulo `presupuesto`. Cubre los procesos canónicos **P-28 (capítulos), P-29 (rubros),
> P-30 (totales/resumen), P-31 (versiones) y P-32 (alertas de integridad)**.
> **No** implementa P-33…P-36 (cronograma), P-37 (export SERCOP), P-09 (duplicar
> proyecto) ni P-38…P-42 (Super-Admin); esas iteraciones viven en
> [`../planes-para-estar-al-dia/`](../planes-para-estar-al-dia/) (planes I-06
> cerrados) y se planificarán aparte cuando llegue su turno (I-08 en adelante).
>
> **Modo de ejecución acordado:** directo sobre `main`, sin SDD/OpenSpec, sin
> crear nuevos worktrees. Cada plan termina en un commit unitario. Las
> verificaciones completas (Spotless, build sin tests, suite completa, Graphify)
> se reservan para el cierre del módulo.
>
> **Convención:** los planes viven como archivos `0N-*.md` enumerados. Cada uno
> es ejecutable por sí solo: estado actual, resultado esperado, dependencias,
> citas canónicas, alcance (paths exactos), secuencia TDD, pasos numerados,
> pruebas y comandos, criterios de terminado, condiciones de parada y unidad de
> revisión. **Esta carpeta NO contiene código ejecutable**; contiene la
> planificación completa para que un ejecutor con cero contexto adicional pueda
> implementarla.

---

## 0. Regla del autor — los planes son baseline revisable (NO muro)

> **Regla del usuario (vinculante para todo el módulo I-07 y siguientes):**
> cada plan de este directorio es un **baseline revisable**, no una muralla
> inmutable. Antes de implementar cualquier plan, el ejecutor **debe**:
>
> 1. **Re-evaluar el plan contra el código y las decisiones canónicas
>    vigentes** (`thesis-docs`, módulos ya cerrados, schema actual,
>    repos/DTOs/services existentes). Comparar el plan escrito con la
>    realidad — no ejecutar el plan en piloto automático.
> 2. Si la re-evaluación detecta desviación entre lo que el plan ordena y
>    lo que el código/schema ya resolvió, el veredicto es
>    **`NEEDS ADJUSTMENT`**: el ejecutor **actualiza el plan ANTES de
>    tocar código** (mismo archivo del plan; actualiza su «Estado de
>    cierre» con las correcciones encontradas, citas textuales al código
>    que las motivó y los ajustes locked para la implementación).
> 3. **No** se fabrican preguntas al humano para mantener el plan
>    intacto: si una duda material (alcance, ownership, contradicción con
>    otro plan, motor cerrado, etc.) no se resuelve con evidencia del
>    repo, el ejecutor para y reporta con `status: interaction_required`.
>    Una duda no resuelta NO es una invitación a improvisar.
> 4. La regla se mantiene vigente para **todos** los planes del módulo
>    (019–025) y para los planes futuros que hereden este patrón. La
>    ejecución directa sobre `main` y la estructura DAG declarada en §3
>    se conservan; lo que cambia es que cada plan puede ajustarse en su
>    propio archivo antes de su implementación.
>
> **Origen de la regla:** identificada durante la re-evaluación de Plan 019
> (2026-08-31). El plan original proponía tests de repositorio redundantes,
> funciones de trigger por tabla, timestamps inexistentes y métodos no
> consumidos. La inspección también confirmó que `SchemaBaselineIT` debe
> conservar su contrato V001-only, por lo que V008 necesita un IT enfocado
> separado, mientras `PublicIdPersistenceTest` sí se amplía como registro
> transversal. La regla queda codificada para que los planes 020–026 no se
> ejecuten en piloto automático ni degraden contratos previos al adaptarse.
>
> **Re-evaluación Plan 025 (2026-09-01):** el plan original proponía
> crear `Actividad.java` JPA, devolver `[]` para `itemsSinActividad`
> cuando no hay cronograma y limitar Bruno a ~6–8 requests. La
> re-evaluación cerró `NEEDS ADJUSTMENT` antes de implementar: sin
> entidad JPA nueva (mirror del deep copy SQL nativo de Plan 024),
> regla `itemsSinActividad` poblada por defecto cuando no hay
> cronograma (TC-P32-04), Bruno con 23 requests (granularidad fina),
> y `0`-comit + `0`-merge por instrucción explícita. Detalle en
> [`00-analisis-reevaluacion.md`](00-analisis-reevaluacion.md).

---

## 1. Resumen ejecutivo

El módulo `presupuesto` introduce el agregado raíz `Presupuesto → Capitulo →
Rubro → APU` (con su `Cronograma`/`Actividad` 1:1 ya previsto en V001 pero
**fuera del alcance de I-07**). Conecta los APUs ya creados (módulo `apu`,
[DONE 2026-08-30]) con la jerarquía administrativa del pliego, totaliza
write-through y serializa un único `PresupuestoResponse` para el frontend.

**Capacidades habilitadas al cierre:**

- Árbol de capítulos jerárquico sin tope (P-28) con `item` autogenerado y
  renumeración atómica al mover (Plan 022 DONE 2026-09-01 — recurso
  dedicado `CapituloResource`, renumeración en dos pasadas con
  entidades gestionadas, STOP (A) activado para ciclos).
- Vínculo 1:1 APU↔rubro por versión (P-29, D-09) con cantidades de obra y
  totales ascendentes.
- Read model completo (`PresupuestoResponse` árbol) + resumen por componente
  (P-30) con totales M/N/O/P, IVA referencial y total general.
- Versionado (P-31): deep copy, vigente única, comparación, eliminación
  protegida de la vigente.
- Validación de integridad (P-32): PU=0, cantidad=0, ítems sin actividad
  (lee `actividad`/`cronograma` con SQL nativo narrow, mirror del deep
  copy Plan 024 — **no** implementa su CRUD, **no** crea entidad JPA
  nueva). Plan 025 **DONE 2026-09-01**.
- Módulo profundo `recalculo` activado (ADR nuevo) — único punto que
  decide qué recalcular tras cada mutación. **Módulo `recalculo` activo
  (Plan 020 DONE 2026-09-01):** interfaz `recalcular(Alcance)` con
  `Alcance = Version | Apu | Insumo`; `Motor` invocado estáticamente;
  pre-requisito = [Plan 015](../../../plans/015-retirar-descuento-apu.md)
  (DONE 2026-09-01), que retiró el campo `ParametrosCalculo.porcentajeDescuento`
  y dejó la columna BD `apu.porcentaje_descuento` como compatibility seam
  inert. Cronología documentada en este `00.md`: Plan 015 → Plan 020 →
  Plan 021 (auto-create v1 vigente, listado y read model del árbol
  `Presupuesto → Capitulo → Rubro`; gap detectado y cerrado por la
  auditoría del plan). DAG I-07 sigue desbloqueado (Plan 022 es el
  siguiente ejecutable).
- Identidad pública UUIDv7 (`public_id UUID DEFAULT uuidv7()`) para
  `capitulo` y `rubro` mediante V008 estructural; PK/FK siguen BIGINT.

**Lo que NO entra en I-07** (documentado explícitamente en cada plan):

- CRUD de cronograma/actividad (P-33…P-36) — vive en I-08/I-09.
- Export SERCOP (P-37) — vive en I-10.
- Duplicar proyecto (P-09) — fuera de alcance desde N02 §3.
- Panel Super-Admin (P-38…P-42) — vive en I-11.
- Cronograma y actividad con identidad pública UUIDv7 propia — diferido a
  migración futura de I-08 (numeración por determinar).
- Cambios de fórmula o tolerancia del motor (motor cerrado por Plan 02 /
  Plan 014 con residual aceptado).
- Módulo nuevo `recalculo` con adaptadores alternativos (sólo el adaptador
  Postgres — ADR 10).

---

## 2. Alcance (qué entra en I-07 y qué no)

### 2.1 Entra

| Proceso | Capacidad | Plan |
|---|---|---|
| P-28 | CRUD capítulos jerárquicos con renumeración atómica | [`04-capitulos.md`](04-capitulos.md) |
| P-29 | CRUD ítems (rubro) vinculados 1:1 a APU, cantidades de obra | [`05-rubros-totales-resumen.md`](05-rubros-totales-resumen.md) |
| P-30 | Totales write-through por capítulo + resumen por componente | [`05-rubros-totales-resumen.md`](05-rubros-totales-resumen.md) |
| P-31 | Versiones (deep copy, vigente única, comparación, eliminar) | [`06-versionado-comparacion.md`](06-versionado-comparacion.md) |
| P-32 | Validación de integridad (PU=0, cantidad=0, sin actividad) | [`07-validacion-y-cierre.md`](07-validacion-y-cierre.md) |
| — | Módulo profundo `recalculo` (ADR nuevo, **ACTIVADO en I-07 / Plan 020 — DONE 2026-09-01**; pre-requisito = Plan 015, ver [`../../../plans/015-retirar-descuento-apu.md`](../../plans/015-retirar-descuento-apu.md)) | [`02-recalculo-write-through.md`](02-recalculo-write-through.md) |
| — | Identidad pública UUIDv7 para `capitulo`/`rubro` (V008 estructural) | [`01-identidad-y-persistencia.md`](01-identidad-y-persistencia.md) |
| — | Auto-create `presupuesto` v1 vigente al crear proyecto (P-06 hoy parcial) | [`03-ciclo-presupuesto.md`](03-ciclo-presupuesto.md) |
| — | Bruno `api/bruno/10-presupuesto/` y Graphify | [`07-validacion-y-cierre.md`](07-validacion-y-cierre.md) |

### 2.2 No entra (documentar fuera de alcance)

| Capacidad | Plan que la difiere |
|---|---|
| P-09 Duplicar proyecto | Excluido por N02 §3 (no se reabre en I-07) |
| P-33…P-36 Cronograma (CRUD, avance, Gantt, desactualización) | I-08 / I-09 |
| P-37 Export SERCOP (.xlsx/.pdf) | I-10 |
| P-38…P-42 Super-Admin | I-11 |
| Identidad pública UUIDv7 para `cronograma` y `actividad` | Diferido a una migración futura de I-08 (numeración por determinar) |
| Cambios de fórmulas del motor, tolerancias, golden expected values | **PROHIBIDO** (Plan 014 §STOP, motor cerrado) |
| Soft-delete genérico, archivado de capítulos | No se introduce |
| Cronograma auto-importado en deep copy estructuralmente pero sin CRUD | Plan 06 lo cubre parcialmente; en I-07 el deep copy sólo copia filas que existan (no crea cronograma nuevo) |

### 2.3 Restricciones globales vigentes (re-impuestas)

- **PK/FK internas siguen `BIGINT`**. `public_id UUID DEFAULT uuidv7()`
  coexiste como identidad externa inmutable (mismo patrón WU-03 que
  `presupuesto`, `apu`, `plantilla_apu`, `plantilla_proyecto`).
- **No-links entre APUs** (N04 §2 / v1.3 §2.5.6) — un APU nunca referencia a
  otro. El módulo `presupuesto` no introduce FKs hacia otros APUs.
- **No** se reabre el motor. La regla workbook-consistent (Plan 014,
  cierre parcial 2026-08-28) sigue vigente con residual aceptado.
- **No** se reabre `insumo.precio_unitario > 0` (catálogo). V007 mantiene
  `rubro.cantidad >= 0` (ya aplicado en I-06).
- **No** se modifica V001–V007. V008 (nueva, estructural) es la única
  migración nueva del módulo.

---

## 3. Orden de ejecución y DAG

```
        ┌──────────────────────────────────────────────────────────────┐
        │                                                              │
        │   Plan 019  Plan 020  Plan 021  Plan 022  Plan 023  Plan 024 │
        │   (01-id)  (02-rec)  (03-vid) (04-cap) (05-rub)  (06-ver)   │
        │      │        │        │        │        │        │          │
        │      ▼        ▼        ▼        ▼        ▼        ▼          │
        │                                                       ▼       │
        │                                              Plan 025        │
        │                                              (07-val)        │
        └──────────────────────────────────────────────────────────────┘
```

### 3.1 Orden recomendado

| # | Plan | Resultado principal | Depende de |
|---:|---|---|---|
| 1 | [`01-identidad-y-persistencia.md`](01-identidad-y-persistencia.md) — Plan 019 | Entidades `Capitulo`/`Rubro` con UUIDv7 público + repositorios | — |
| 2 | [`02-recalculo-write-through.md`](02-recalculo-write-through.md) — Plan 020 | Módulo `recalculo` activo: convierte «qué cambió» → write-through | 019 |
| 3 | [`03-ciclo-presupuesto.md`](03-ciclo-presupuesto.md) — Plan 021 | Auto-create `presupuesto` v1 vigente en `POST /proyectos`; listado + detalle vigente | 019 |
| 4 | [`04-capitulos.md`](04-capitulos.md) — Plan 022 | CRUD capítulos + mover atómico + ciclo prevention | 019, 020, 021 |
| 5 | [`05-rubros-totales-resumen.md`](05-rubros-totales-resumen.md) — Plan 023 | CRUD rubros + totales + resumen M/N/O/P + consolidación vía `recalculo` | 019, 020, 022 |
| 6 | [`06-versionado-comparacion.md`](06-versionado-comparacion.md) — Plan 024 | Deep copy + vigente + comparación + eliminación protegida | 019, 020, 022, 023 |
| 7 | [`07-validacion-y-cierre.md`](07-validacion-y-cierre.md) — Plan 025 | Validación + Bruno `10-presupuesto/` + Graphify + cierre | 019–024 |

### 3.2 Reglas del DAG

- **019** no tiene dependencias: agrega identidad pública y entidades JPA.
- **020** depende de 019 (necesita `Capitulo`/`Rubro` para propagar) pero
  **no** requiere 022 ni 023; implementa el motor write-through con
  `Motor.consolidar` ya vigente.
- **021** depende de 019 (presupuesto ya existe como entidad, sólo falta el
  servicio para crear v1 al crear proyecto y los read models); no requiere
  `recalculo` activo porque `presupuesto.total = 0` por default al inicio.
- **022/023** dependen de 019 + 020 + 021: los totales write-through de
  capítulos/rubros/presupuesto exigen `recalculo` activo.
- **024** depende de 019–023 (deep copy del árbol completo + remapeo de FKs).
- **025** es el cierre: depende de todos los anteriores para validar la
  integridad cross-cutting (PU=0, cantidad=0, sin actividad).

---

## 4. Fuentes canónicas (referencias vivas)

Estos archivos son **citados textualmente** por cada plan; no se duplica su
contenido. Cualquier cambio en una fuente canónica debe reflejarse en el plan
que la cita.

| Documento | Cita usada por |
|---|---|
| [`../../../../thesis-docs/plan/architecture/06-database-schema.md`](../../../../thesis-docs/plan/architecture/06-database-schema.md) §2.8–§2.11 | 019 (V008 capitulo/rubro public_id), 022 (capitulo jerarquía), 023 (rubro), 024 (deep copy remapeo) |
| [`../../../../thesis-docs/plan/architecture/07-api-contract.md`](../../../../thesis-docs/plan/architecture/07-api-contract.md) §6 (P-28…P-32), Apéndice B | 019, 021, 022, 023, 024, 025 |
| [`../../../../thesis-docs/plan/architecture/08-codebase-design.md`](../../../../thesis-docs/plan/architecture/08-codebase-design.md) §1, §3, §6 (módulo `recalculo`) | 020 (activación de `recalculo`) |
| [`../../../../thesis-docs/plan/domain/02-data-model.md`](../../../../thesis-docs/plan/domain/02-data-model.md) §3 (Presupuesto), §4 (Capitulo), §5 (Rubro), §13 (Cronograma), §16 (fórmulas), §17 #1, #6, #7, #14, #15 | 019, 021, 022, 023, 024, 025 |
| [`../../../../thesis-docs/plan/design/03-procesos-detalle.md`](../../../../thesis-docs/plan/design/03-procesos-detalle.md) §E P-28…P-32 | 019, 021, 022, 023, 024, 025 |
| [`../../../../thesis-docs/plan/quality/02-catalogo-pruebas.md`](../../../../thesis-docs/plan/quality/02-catalogo-pruebas.md) §E P-28…P-32 | 019, 021, 022, 023, 024, 025 |
| [`../../../../thesis-docs/plan/quality/02-catalogo-pruebas.md`](../../../../thesis-docs/plan/quality/02-catalogo-pruebas.md) §Plan 07 UUIDv7 + Tabla Insumo/Base/Plantilla (patrón WU-03 replicado) | 019 |
| [`plans/014-motor-precision-no-links.md`](../../../plans/014-motor-precision-no-links.md) + `plans/006-motor-consolidacion-fix.md` (cierre parcial user-accepted 2026-08-28) | 020 (motor no se reabre) |
| [`../planes-para-estar-al-dia/08-cierre-documental-y-verificacion.md`](../planes-para-estar-al-dia/08-cierre-documental-y-verificacion.md) (Plan 08 cierre — patrón aplicado) | 025 |
| [`../planes-para-estar-al-dia/04-plantillas-apu.md`](../planes-para-estar-al-dia/04-plantillas-apu.md) y `06-plantillas-proyecto.md` — patrón de UUIDv7 en frontera | 019, 025 |

**Decisiones cerradas** que aplican al módulo (no se reabren):

- A1 / N04 §A1 — descuento: FORMA 1 (mutación columnas base PROYECTO) +
  FORMA 2 (edición atómica); MO exenta; **nunca monto absoluto**.
- A2 / N04 §2 / v1.3 §2.5.6 — no-links entre APUs.
- A3 — HM primera fila del bloque M por defecto, reordenable.
- A6 / N04-bis — rangos configurables vía `ParametrosSistema`.
- A8 / N04 §A8 — plantilla de proyecto (ya implementado en Plan 06).
- A9 / N04 §A9 — bases siempre se copian a PROYECTO.
- D-04 / N02 §3 — `POST /proyectos/{id}/duplicar` **NO** se construye (P-09
  excluido). El deep copy de versión (P-31) cubre el caso de uso real
  manteniendo historial.
- D-09 — `Rubro.apu_id UNIQUE` por versión (1:1 APU↔rubro).
- D-12 — archivar/borrar central sin bloqueo (ya implementado en Plan 05).
- GM-19/GM-20 con residual aceptado (Plan 014 §6, user-accepted 2026-08-28):
  **no** se reabre el motor para cerrarlo.

---

## 5. Invariantes globales (a respetar en cada plan)

1. **Identidad pública UUIDv7.** Toda identidad que cruza la frontera REST va
   como `id: <UUIDv7>` (no `publicId`/`public_id`). Las PK/FK internas siguen
   `BIGINT`. La columna `public_id UUID DEFAULT uuidv7()` se añade **solo**
   en V008 (capitulo + rubro) y en migraciones futuras para `cronograma` y
   `actividad` (migración futura de I-08, fuera de I-07). El trigger de inmutabilidad de
   `public_id` se crea en V008 estructural.
2. **Autorización owner-scoped (RNF-05).** Recursos ajenos → 404 (nunca 403).
   Recurso inexistente o UUID ajeno → `404 no-encontrado`. UUID malformado o
   no-v7 → `400 validacion`.
3. **Write-through atómico.** Toda mutación del agregado
   `Presupuesto → Capitulo → Rubro` recalcula en la misma transacción: total
   del rubro (`precio_total = cantidad × PU_2dp`), totales de capítulos
   ascendentes (recursivos), `presupuesto.total` (Σ de capítulos raíz).
   Utiliza `recalculo` (Plan 020) que llama a `Motor.consolidar` con el
   `VersionSnapshot` reducido.
4. **Cantidad de rubro — API ≥ 0 (no > 0).** V007 ya relaja el CHECK
   `cantidad > 0` → `cantidad >= 0` en la BD para soportar reconstrucción
   estructural desde plantilla de proyecto. **La API REST P-29 sigue exigiendo
   `cantidad > 0`** vía los DTOs (`RubroCrearRequest`, `RubroPatchRequest`).
   El relajamiento es estrictamente interno al path de plantillas (Plan 06).
   Esta doble política se documenta en 023.
5. **No-links entre APUs.** El módulo `presupuesto` **no** introduce
   `Rubro → APU` auxiliar; un rubro siempre referencia un APU ordinario
   mediante `apu_id BIGINT` (FK simple, ya en V001 §2.11).
6. **Auto-create presupuesto v1 vigente (P-06 §4 — hoy parcial).** El
   servicio `ProyectoService.crear` ya crea la fila `presupuesto` (versión 1
   vigente) dentro de la transacción; I-07 lo verifica y lo deja como
   contrato (no se introduce lógica nueva — sólo se documenta como parte
   del módulo `presupuesto`).
7. **Deep copy preserva totales idénticos al origen** (TC-P31-01, P-31 §3):
   `presupuesto.total` y todos los `capitulo.total`/`rubro.precio_total`
   coinciden bit-a-bit entre versión origen y copia. Los insumos base NO
   se copian (la base es del proyecto, compartida entre versiones).
8. **Versionado 1:1 cronograma.** Una versión tiene 0 o 1 cronograma. El
   deep copy de versión **copia estructuralmente** las filas `cronograma` y
   `actividad` si existen (DM §3) **sin** implementar su CRUD (I-08).
9. **Motor intacto.** El motor opera con `BigDecimal` natural y la regla
   workbook-consistent en `internal/Consolidador.java`. La única rounding
   del motor es la frontera APU→Rubro (`precioUnitario DOWN 2dp`,
   `precioTotal = cantidad × PU_2dp` retenido a escala 6 `HALF_UP`). El
   módulo `presupuesto` no importa ni reescribe nada del motor.
10. **No hay tercer módulo de seam.** `recalculo` es el único seam nuevo de
    I-07 (ADR-10 sigue vigente: sin puertos de repositorio; sólo el puerto
    de correo es costura legítima).

---

## 6. Estructura de paquetes esperada (orientativa)

Refleja la convención de los módulos ya cerrados (`proyecto`, `insumo`,
`apu`, `plantilla`):

```
src/main/java/ec/uce/propuestas/presupuesto/
├── entity/
│   ├── Presupuesto.java      # (existente, ya con publicId UUIDv7 — WU-03)
│   ├── Capitulo.java         # NUEVO (Plan 019)
│   └── Rubro.java            # NUEVO (Plan 019)
├── repository/
│   ├── PresupuestoRepository.java   # (existente, WU-03)
│   ├── CapituloRepository.java      # NUEVO (Plan 019, 022)
│   └── RubroRepository.java         # NUEVO (Plan 019, 023)
├── dto/
│   ├── PresupuestoVersionResponse.java          # NUEVO (021)
│   ├── PresupuestoResponse.java                 # NUEVO (021, 022, 023)
│   ├── ResumenComponentesResponse.java          # NUEVO (023)
│   ├── ComparacionVersionesResponse.java        # NUEVO (024)
│   ├── ValidacionPresupuestoResponse.java       # NUEVO (025)
│   ├── RubroRefResponse.java                    # NUEVO (025)
│   ├── CapituloResponse.java                    # NUEVO (022)
│   ├── RubroResponse.java                       # NUEVO (023)
│   ├── CapituloCrearRequest.java                # NUEVO (022)
│   ├── CapituloEditarRequest.java               # NUEVO (022)
│   ├── CapituloMoverRequest.java                # NUEVO (022)
│   ├── RubroCrearRequest.java                   # NUEVO (023)
│   ├── RubroPatchRequest.java                   # NUEVO (023)
│   ├── PresupuestoVersionCrearRequest.java       # NUEVO (024)
│   └── ...mappers estáticos en mapper/
├── mapper/
│   ├── CapituloMapper.java                      # NUEVO (022)
│   └── RubroMapper.java                         # NUEVO (023)
├── service/
│   ├── PresupuestoService.java                  # NUEVO (021)
│   ├── VersionadoService.java                   # NUEVO (024)
│   ├── CapituloService.java                     # NUEVO (022)
│   ├── RubroService.java                        # NUEVO (023)
│   ├── ValidacionPresupuestoService.java        # NUEVO (025)
│   └── ...internals package-private para reglas puras
└── resource/
    ├── PresupuestoResource.java                 # NUEVO/ampliado (021, 023)
    ├── PresupuestoVersionResource.java          # NUEVO (024)
    ├── PresupuestoValidacionResource.java       # NUEVO (025)
    ├── CapituloResource.java                    # NUEVO (022)
    └── RubroResource.java                       # NUEVO (023)

src/main/java/ec/uce/propuestas/recalculo/   # NUEVO módulo profundo (020)
├── Alcance.java                  # sealed/record con casos apu/version/capitulo/rubro
├── RecalculoService.java
└── internal/
    └── VersionSnapshotBuilder.java

src/main/resources/db/migration/
└── V008__capitulo_rubro_public_id.sql   # NUEVO estructural (019)
```

**Reglas de la organización** (alineadas con `docs/modulos/README.md`):

- DTOs `record` Java + `@Valid`; mappers estáticos en `mapper/`.
- `RestResponse<T>` de RESTEasy Reactive cuando el código ya lo usa;
  mantener `Response` JAX-RS en recursos existentes (desvío histórico
  documentado — no se reescribe en I-07).
- Repos `PanacheRepositoryBase` por entidad (`repository/`).
- Errores `ProblemaException` + `GlobalExceptionMapper` de `common/`.
- CRUD plano (`Resource → entity Panache`) cuando no hay reglas; `Service`
  cuando hay invariantes no triviales (P-28 jerarquía, P-29 write-through,
  P-31 deep copy).
- Sin `Lombok`, sin `MapStruct`, sin `double`/`float`.

---

## 7. Estado explícito (planning vs implementado)

| Capacidad | Estado al cierre previsto | Quién cierra |
|---|---|---|
| Entidades `Capitulo`/`Rubro` con UUIDv7 público | **IMPLEMENTED** (Plan 019) | 019 |
| Repos `CapituloRepository`/`RubroRepository` con owner-scope | **IMPLEMENTED** (Plan 019) | 019 |
| Migración V008 (public_id UUIDv7 en capitulo y rubro) | **IMPLEMENTED** (Plan 019) | 019 |
| Módulo `recalculo` activo (único seam de write-through) | **IMPLEMENTED** (Plan 020, 2026-09-01, `Alcance = Version | Apu | Insumo`; pre-requisito = Plan 015 DONE) | 020 (DONE) |
| Auto-create `presupuesto` v1 vigente en `POST /proyectos` | **IMPLEMENTED** (Plan 021, 2026-09-01; gap detectado por auditoría, cerrado por este plan: `ProyectoService.crear` invoca `PresupuestoService.crearVigenteInicial` en la misma `@Transactional`; sin seam nuevo) | 021 |
| `GET /proyectos/{id}/presupuestos` (lista de versiones, owner-scoped) | **IMPLEMENTED** (Plan 021, 2026-09-01; servido por `ProyectoResource`) | 021 |
| `GET /presupuestos/{id}` (read model recursivo completo del árbol `Presupuesto → Capitulo → Rubro`) | **IMPLEMENTED** (Plan 021, 2026-09-01; servido por `PresupuestoResource`; ruta split por cohesión) | 021 |
| CRUD capítulos + mover atómico (P-28) | **IMPLEMENTED** (Plan 022, 2026-09-01; `CapituloResourceIT` 30/30, `PresupuestoResourceIT` 9/9, `RecalculoServiceIT` 4/4, `apu.*` 49/49 verdes, motor con residuales aceptados) | 022 |
| CRUD rubros + write-through totales ascendentes (P-29) | **IMPLEMENTED** (Plan 023) | 023 |
| `GET /presupuestos/{id}/resumen` (P-30) | **IMPLEMENTED** (Plan 023) | 023 |
| Versiones (deep copy, vigente, comparación, eliminar) (P-31) | **IMPLEMENTED** (Plan 024) | 024 |
| Validación PU=0 / cantidad=0 / sin actividad (P-32) | **DONE** (Plan 025, 2026-09-01; `PresupuestoValidacionResourceIT` 13/13; suite completa 438 = 435 pass + 2 aceptados + 1 skipped + 0 errors) | 025 |
| Bruno `api/bruno/10-presupuesto/` | **DONE** (Plan 025, 2026-09-01, 23 requests autocontenidos, 23/23 verde dinámico contra PostgreSQL 18 limpio + fast-jar, 83/83 tests, 0 failures/errors/skips, Bruno CLI 4.1.0) | 025 |
| Graphify refresh + cierre documental | **DONE** (Plan 025, 2026-09-01; preflight `graphify query` ejecutado; final `graphify update .` **DONE — 2.972 nodos / 9.064 aristas / 141 comunidades**) | 025 |

**Distinción planning / implementación:**

- Las columnas «**PLANNED**» describen lo que cada plan **ordena construir**
  durante su ejecución. Aún no existe código (salvo lo marcado «**DONE**»).
- Las columnas «Estado al cierre previsto» describen lo que **se espera**
  observar al cierre de cada plan. El ejecutor las actualiza a
  «**IMPLEMENTED**», «**PARTIAL**» o «**BLOCKED**» en la sección
  «Estado de cierre» del plan correspondiente.

---

## 8. Tabla de status (snapshot inicial)

> Esta tabla se actualiza al cierre de cada plan (en su sección
> «Estado de cierre»). El estado «PLANNED» significa **no iniciado**;
> «READY» significa **diseñado, listo para ejecutor**; «IMPLEMENTED»
> significa **código + tests + verificación dirigida verde**; «PARTIAL»
> significa **algunos cierres pendientes documentados**; «BLOCKED»
> significa **STOP condition disparada, escalate al autor**.

| Plan | Iteración | Baseline | Estado actual |
|---|---|---|---|
| 019 Identidad y persistencia | I-07 | PLANNED / READY | **DONE · verificación dirigida completa** |
| 020 Recalculo write-through | I-07 | PLANNED / READY (pre-Plan 015) | **DONE · 2026-09-01.** `Motor.consolidar(VersionSnapshot)` ya no exige `porcentajeDescuento` global (retirado por Plan 015); módulo `ec.uce.propuestas.recalculo` activo con `Alcance = Version | Apu | Insumo` (sealed interface). `Motor` invocado estáticamente; `VersionSnapshotBuilder` público por límite de subpaquete Java (no es seam). `RecalculoServiceIT` 4/4 verdes. DAG I-07 desbloqueado. |
| 021 Ciclo presupuesto | I-07 | PLANNED / READY | **DONE · 2026-09-01 · verificación dirigida completa y recheck post-fixture-only finalizado.** Auto-create v1 vigente en `POST /proyectos` orquestado desde `ProyectoService.crear` (gap detectado por la auditoría del plan, cerrado en este mismo plan sin seam nuevo); `GET /proyectos/{id}/presupuestos` (lista de versiones) servido por `ProyectoResource`; `GET /presupuestos/{id}` (read model recursivo del árbol) servido por `PresupuestoResource`; DTOs `PresupuestoVersionResponse` / `PresupuestoResponse` / `CapituloResponse` / `RubroResponse`; `alertas` diferido a Plan 025; `ParametrosProyecto` / `BaseInsumos` lazy. **Evidencia (medida tras retoques fixture-only):** `PresupuestoResourceIT` **9/9**, `ProyectoResourceIT` **8/8** (TC-P06-01 verifica el auto-create), `RecalculoServiceIT` **4/4** verde (sin regresión); `./gradlew spotlessCheck` **verde**, `./gradlew build -x test` **verde**, `git diff --check` **limpio**; suite completa **346 totales = 343 pass + 2 aceptados (GM-19 `-$6.95`, GM-20 cap. 1 `-$0.84`) + 1 skipped (GM-24 `@Disabled`) + 0 errors**. DAG I-07 sigue desbloqueado. |
| 022 Capítulos | I-07 | PLANNED / READY | **DONE · 2026-09-01 · verificación dirigida completa.** P-28 implementado en `CapituloResource` (POST/PUT/PATCH …/mover/DELETE) con `presupuestoId` en cada ruta; numeración jerárquica autogenerada; renumeración atómica en dos pasadas (entidades gestionadas, item temporal `~<id>`) que respeta `UNIQUE (presupuesto_id, item)` durante el flush; detección de ciclos self/descendiente con `CapituloRepository.esDescendienteOigual` (STOP (A) activado — recorrido `findById` en memoria en lugar de `WITH RECURSIVE`); cascade Postgres en `capitulo.parent_id` + `rubro.capitulo_id`, APUs sobreviven (D-09 + V001 §3); cada mutación termina con `RecalculoService.recalcular(new Alcance.Version(presupuestoId))` y devuelve el árbol completo (`PresupuestoResponse`). DTOs con `@Size(max=255)` en `descripcion` y validación explícita de `orden` en `[1, hermanos+1]`. **Evidencia:** `CapituloResourceIT` 30/30, `PresupuestoResourceIT` 9/9, `RecalculoServiceIT` 4/4, `apu.*` 49/49; motor 45 = 42 verdes + 2 rojos aceptados (GM-19/GM-20) + 1 skipped (GM-24); suite completa **376 = 373 pass + 2 aceptados + 1 skip + 0 errors**; `./gradlew spotlessCheck` verde, `./gradlew build -x test` verde, `git diff --check` limpio. DAG I-07 sigue desbloqueado. |
| 023 Rubros, totales, resumen | I-07 | PLANNED / READY | **DONE · 2026-09-01.** CRUD P-29 en `RubroResource`; D-09 y cantidad > 0; write-through por `Alcance.Version`; resumen P-30 read-only con M/N/O/P directos, `totalGeneral` persistido e IVA sobre ese total. Evidencia: rubros 19/19, resumen 12/12, capítulos 31/31, read model 9/9 y cálculo read-only 3/3; suite completa 411 = 408 verdes + GM-19/GM-20 aceptados + GM-24 omitido; Spotless/build/diff verdes. |
| 024 Versionado y comparación | I-07 | PLANNED / READY | **DONE · 2026-09-01 · cierre documental ejecutado en este pase.** P-31 implementado: deep copy bit-a-bit idéntico al origen (TC-P31-01 tolerancia 0.00), marcar vigente transaccional, eliminar con protección de la vigente (409 `version-vigente-protegida`), comparación lado a lado. Toda la implementación vive bajo `ec.uce.propuestas.presupuesto.{dto,resource,service}` (`VersionadoService`, `PresupuestoVersionResource`, `PresupuestoVigenciaResource`, `ComparacionResource`); `PresupuestoRepository` gana `maxVersionDeProyecto` y `lockProyectoRow` (data-access only, sin seam en `proyecto/`). Deep copy con UUIDv7 públicos frescos y FKs BIGINT internas remapeadas; `cronograma` y `actividad` se copian con **SQL nativo** dentro de `VersionadoService` (entidades JPA viven en I-08/I-09; sin entidades nuevas, sin migraciones, sin seam); columna inert `apu.porcentaje_descuento` (Plan 015) no se mapea/copia. Lock pesimista de fila `proyecto` serializa `max(version)+1` y toggles `es_vigente`; índice único parcial `ux_presupuesto_vigente` (V001 §2.8) preservado. `owner-to-404` (RNF-05), D-09 y UUIDv7 en frontera REST preservados. **Evidencia (medida):** `VersionadoResourceIT` **14/14 verde**; tests de deep-copy **0.785 s** y **0.850 s** sobre fixture compacto representativo (sin benchmark específico del árbol IESS completo); `./gradlew test --tests 'ec.uce.propuestas.presupuesto.*' ...` → **85/85 verde**; regresiones `apu.*` y `recalculo.*` verdes; motor **45 = 42 verdes + 2 rojos aceptados (GM-19 `-$6.95`, GM-20 cap. 1 `-$0.84` — Plan 014, no se reabre el motor) + 1 skipped (GM-24 `@Disabled`)**; `./gradlew spotlessCheck` verde; `./gradlew build -x test` **BUILD SUCCESSFUL** (31 deprecation warnings preexistentes, sin error); `./gradlew test` (suite completa) → **425 totales = 422 verdes + 2 rojos aceptados (GM-19 + GM-20) + 1 skipped (GM-24) + 0 errors**. `git diff --check` limpio y `graphify update .` finalizado correctamente. **No** se ejecutó commit unitario (instrucción explícita). **No** se modificó `motor/`, `recalculo/`, `apu/`, `proyecto/` fuente ni migraciones; **no** se avanzó Plan 025. |
| 025 Validación y cierre | I-07 | PLANNED / READY | **DONE (2026-09-01).** P-32 implementado en este pase: `ValidacionPresupuestoService.validar(UUID, Long) → ValidacionPresupuestoResponse{exportable, itemsPuCero[], itemsCantidadCero[], itemsSinActividad[]}` (PU desde `rubro.precio_unitario`, cantidad desde `rubro.cantidad`, sin actividad desde `PresupuestoRepository.findRubrosCubiertosPorCronograma` con SQL nativo narrow, mirror del deep copy Plan 024 — sin entidad JPA `Actividad`/`Cronograma`, sin V009, sin seam); `PresupuestoValidacionResource` con `GET /presupuestos/{presupuestoId}/validacion` (UUIDv7 validado en frontera con `UuidV7.parse`, owner-scope, errores 400 `validacion` / 404 `no-encontrado`, roles `USUARIO`/`SUPER_ADMIN`, sin `@Transactional` read-only); DTOs `ValidacionPresupuestoResponse` y `RubroRefResponse` (4 campos exactos: `id` UUIDv7, `item`, `codigo`, `descripcion` — sin fugas de `BIGINT` ni del `RubroResponse`); `PresupuestoValidacionResourceIT` con **13 tests escritos primero** (TC-P32-01..13: defectos ortogonales, presupuesto vacío, versión válida, sin cronograma ⇒ todos los rubros sin actividad, defectos múltiples en un rubro, orden determinista, forma `RubroRefResponse` 4 campos sin fugas, UUID malformado, UUIDv4 → 400, UUIDv7 inexistente → 404, caller ajeno → 404, aislamiento cross-presupuesto, read-only tras N invocaciones). Bruno `api/bruno/10-presupuesto/` autocontenido (23 requests: 5 helpers `TC-10-00a..00e` + 18 casos temáticos `TC-10-01..15` con P-28 desglosado en 4 sub-casos `TC-10-02a..02d`, P-29/P-30/P-31/P-32 + 3 negativos `TC-10-13..15` cubriendo UUIDv7/owner-scope) con `folder.bru` propio y `api/bruno/environments/dev.bru` extendido con vars runtime `p25*` (no se crea `10-presupuesto/environments/`, Bruno carga el environment compartido). Bruno dinámico contra PostgreSQL 18 limpio + fast-jar: **23/23 requests, 83/83 tests asserts, 0 failures/errors/skips, 4.554 s CLI / 6.070 s wall, Bruno CLI 4.1.0**. `graphify query` preflight ejecutado; `graphify update .` final **DONE — 2.972 nodos / 9.064 aristas / 141 comunidades**. `api/bruno/README.md` actualizado con tabla de todas las colecciones y guía de ejecución de `10-presupuesto/`; `docs/modulos/05-presupuesto/00-analisis-reevaluacion.md` con `NEEDS ADJUSTMENT` cerrado antes de implementar (sin `Actividad.java` JPA, regla `itemsSinActividad` poblada por defecto cuando no hay cronograma, 23 requests Bruno en lugar de ~6–8). Documentación canónica sincronizada: `00.md` §0/§8/§10, `README.md` (fila `05-presupuesto`), `00-ESTADO-ACTUAL.md` §2/§3, `estado-actual.md` bloque I-07, `CLAUDE.md`. **NO** se ejecuta commit unitario ni merge (instrucción explícita). **NO** se reabre motor / V001–V008 / `recalculo` / `apu` / `proyecto/`. **Evidencia medida (2026-09-01):** `PresupuestoValidacionResourceIT` 13/13; `./gradlew test --tests 'ec.uce.propuestas.presupuesto.*'` 98/98; `./gradlew test --tests 'ec.uce.propuestas.apu.*'` 52/52; `./gradlew test --tests 'ec.uce.propuestas.recalculo.*'` 4/4; motor 45 = 42 pass + 2 aceptados (GM-19 `-$6.95`, GM-20 cap. 1 `-$0.84`) + 1 skipped (GM-24 `@Disabled`) + 0 errors; suite completa **438 = 435 pass + 2 aceptados + 1 skipped + 0 errors**; `./gradlew spotlessCheck` PASS; `./gradlew build -x test` PASS; `git diff --check` limpio. Sin bloqueos correctivos identificados en Plans 019–024; las dos observaciones de auditoría no son defectos: `TRUNCATE ... CASCADE` cubre el reset de `cronograma`/`actividad` (no entidades JPA en este módulo) y D-09 (1:1 APU↔rubro por versión) implica que los códigos APU son únicos por presupuesto. Tras cierre Plan 025, la **siguiente tarea de planificación** es **Plan 026 (I-08 — cronograma CRUD)**, que aún **no** existe como archivo ejecutable y debe autorarse en su propia sesión. |
| **Módulo `presupuesto` I-07** | I-07 | **DAG I-07 cerrado** | **DONE · 8/8 planes DONE (015, 019, 020, 021, 022, 023, 024, 025) — 2026-09-01.** Evidencia medida: suite completa **438 = 435 pass + 2 aceptados (GM-19 `-$6.95`, GM-20 cap. 1 `-$0.84`) + 1 skipped (GM-24 `@Disabled`) + 0 errors**; `./gradlew spotlessCheck` PASS; `./gradlew build -x test` PASS; `git diff --check` limpio; Bruno dinámico `10-presupuesto/` 23/23 verde. La **siguiente tarea de planificación** es Plan 026 (I-08 — cronograma CRUD), que aún **no** existe como archivo ejecutable y debe autorarse en su propia sesión. |

---

## 9. Cómo trabajar este módulo

1. **Un solo plan a la vez**, respetando las dependencias declaradas.
2. Confirmar primero que el código y las decisiones canónicas no cambiaron
   desde el corte de [`../estado-actual.md`](../estado-actual.md) y
   [`../../../docs/00-ESTADO-ACTUAL.md`](../../../docs/00-ESTADO-ACTUAL.md).
3. Aplicar **únicamente** el alcance indicado en cada plan; cualquier
   necesidad de crear un módulo nuevo fuera de `recalculo` es condición de
   parada.
4. Ejecutar las **pruebas específicas** del plan antes de continuar con el
   siguiente (verificación dirigida; la suite completa y Graphify quedan
   para Plan 025).
5. Cada plan termina con un commit unitario + actualización de su
   «Estado de cierre» + nota aquí en §8.

---

## 10. Siguiente tarea de planificación

Planes 015 y 019–025 están **DONE** (2026-09-01); el árbol completo,
write-through, CRUD de capítulos/rubros, resumen P-30, versionado P-31
(deep copy + vigente + comparación + eliminación protegida) y validación
de integridad P-32 están activos y verificados con conteo real desde
XML (suite completa 438 = 435 pass + 2 aceptados + 1 skipped + 0 errors;
Spotless PASS; `build -x test` PASS; `git diff --check` limpio; Bruno
dinámico `10-presupuesto/` 23/23).

La **siguiente tarea de planificación** es **Plan 026 (I-08 — cronograma
CRUD)**, que aún **no** existe como archivo ejecutable y debe autorarse
en su propia sesión cuando llegue su turno. P-33…P-36 (CRUD y avance de
`cronograma`/`actividad`) viven en I-08/I-09 y quedan explícitamente
diferidos al cierre de Plan 025.

---

## 11. Condiciones de parada globales (módulo)

Detener y reportar al autor si:

- Cualquier plan necesita **reabrir el motor** (`Motor.java`,
  `internal/CalculadorFila.java`, `internal/Consolidador.java`,
  fixtures GM, tolerancias o expected values). **STOP.**
- Cualquier plan necesita **modificar V001–V007**. **STOP** — sólo V008
  (estructural) está autorizada en 019.
- Cualquier plan necesita **introducir un módulo nuevo de primer nivel**
  distinto de `recalculo`. **STOP.**
- Cualquier plan necesita **cambiar el contrato API UUIDv7** ya cerrado
  por Plan 07 (cualquier recurso distinto de `presupuesto`/`capitulo`/
  `rubro`/`cronograma`/`actividad` ya migrado). **STOP.**
- Aparece evidencia nueva de que el **residual sub-céntimo de GM-19/GM-20**
  no es atribuible al example workbook IESS. **STOP** — escalar antes de
  tocar cualquier código del motor.
- Cualquier plan necesita **PK/FK distintas de `BIGINT`** para `capitulo`/
  `rubro`. **STOP** — la identidad interna sigue siendo `BIGINT` (la
  externa es UUIDv7 vía `public_id`).
- Aparece necesidad de **soft-delete genérico** para capítulos o rubros.
  **STOP** — se borra físicamente con RESTRICT/CASCADE según el DDL
  canónico (V001 §3).

## 12. Convenciones de cada plan

Cada `0N-*.md` sigue esta plantilla (las secciones en **negrita** son
obligatorias):

1. **Resultado esperado** — qué hace el sistema al cierre.
2. **Dependencias** — qué planes deben estar cerrados antes.
3. **Estado de cierre** — inicialmente «PLANNED / READY», actualizado al
   cierre por el ejecutor (DONE/PARTIAL/BLOCKED con explicación breve).
4. **Contexto / estado actual** — qué existe en código y qué falta
   (citas textuales a `docs/modulos/03-apu.md`, `04-apu-avanzado.md`,
   `planes-para-estar-al-dia/`).
5. **Alcance** — Incluye / No incluye (paths exactos).
6. **Contrato esperado** — endpoints, DTOs, errores, códigos HTTP.
7. **Pasos numerados** — uno a uno, sin ambigüedades.
8. **Pruebas y comprobaciones** — comandos exactos con resultado esperado.
9. **Criterios de terminado** — checklist verificable.
10. **STOP conditions** — condiciones específicas del plan (las globales
    viven en §11 arriba).
11. **Siguiente plan ejecutable** — link al siguiente del DAG.

Todas las plantillas viven en español (convención del repositorio). Las
citas a canonical sources se hacen con rutas relativas al archivo
`docs/modulos/05-presupuesto/` desde donde se leen.
