meta {
  name: TC-06-12 — Eliminar proyecto
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/proyectos/{{proyectoId}}
  auth: bearer
}

auth:bearer {
  token: {{u1_access_token}}
}

tests {
  test("Status 204 No Content", function() {
    expect(res.status).to.equal(204);
  });
}
