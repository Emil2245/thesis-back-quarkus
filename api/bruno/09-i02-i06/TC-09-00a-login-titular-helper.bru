meta {
  name: TC-09-00a — Login titular (helper)
  type: http
  seq: 0
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{u1Email}}",
    "password": "{{u1Password}}",
    "recordarSesion": true
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("u1_access_token", res.body.accessToken);
  }
}

tests {
  test("Status 200 OK", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Helper inicial para los casos Plan 08 — captura el access token
  del titular (John Doe). Debe ejecutarse antes que cualquier caso
  autenticado de esta carpeta.
}