name: thesis-backend
services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: propuestas
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5436:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
volumes:
  postgres_data: